@using System.Text.Json;
@* @{
    var data = Outputs
            .FromPipeline("Data")
            .FilterSources("data/*")
            .FirstOrDefault(s => s.Source.ToString().EndsWith("topMenu.json"));

    var topMenuItemsRaw = data.Get<object[]>("MenuItems");

    var topMenu = new List<TopMenuItem>();

    foreach (var item in topMenuItemsRaw)
    {
        string serialized = JsonSerializer.Serialize(item);
        var topMenuItem = JsonSerializer.Deserialize<TopMenuItem>(serialized);
        topMenu.Add(topMenuItem);
    }    
} *@
<nav class="navbar navbar-expand-lg bg-light sticky-top">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="offcanvas" data-bs-target="#mainNavigation" aria-controls="mainNavigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <a class="navbar-brand" href="/">L5R</a>
        <div></div>
        @*
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                @{
                    foreach (var item in topMenu)
                    {
                        var isDropdown = (item.Children?.Count() ?? 0) > 0;
                        if (!isDropdown)
                        {
                            <li class="nav-item">
                                <a class="nav-link" aria-current="page" href="@item.Path">@item.Title</a>
                            </li>
                        } 
                        else
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="@item.Path" role="button" data-bs-toggle="dropdown"
                                    aria-expanded="false">
                                    @item.Title
                                </a>
                                <ul class="dropdown-menu">
                                    @{
                                        foreach (var childItem in item.Children)
                                        {
                                            <li><a class="dropdown-item" href="@childItem.Path">@childItem.Title</a></li>
                                        }
                                    }
                                </ul>
                            </li>
                        }
                    }
                }
        </div> *@
    </div>
</nav>